#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define lli long long int


int main()
{	
    int contributor;
    int projects;
    cin >> contributor >> projects;
    while(contributor --)
    {
        string name;
        int n;
        cin >> name >> n;
        cin.ignore();
        while(n--)
        {
            string skill;
            int level;
            cin >> skill >> level;
            cin.ignore();
        }

    }
    while(projects --)
    {
        string project;
        int days, score, best_before, roles;
        cin >> project >> days >> score >> best_before >> roles;

        while(roles --)
        {
            string skill_name;
            int skill_level;
            cin >> skill_name >> skill_level;
        }

        bool visited[projects];
        while()

    }

    return 0;
}